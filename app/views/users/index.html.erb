<% provide(:title, @title ) %>
<% provide(:active, 'user' ) %>

<% content_for :body do %>

  <%= render partial: 'search',locals: { users: @users } %>

  <%= render partial: 'users',locals: { users: @users } %>

<% end %>
