
<% content_for :body do %>
  

  <div class="container-fluid">
    <div class="row">
      <%= render partial: 'main_side_bar', locals: { active: 'administration' } %>
      <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
        <h1 class="page-header"><%= @title %></h1>

        <%= render partial: 'main_buttons' %>

        <%= render partial: 'ultimos_pedidos', locales: @sales %>

      </div>
    </div>
  </div>
<% end %>
